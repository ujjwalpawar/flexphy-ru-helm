{{- /*
  DU-Low ConfigMaps are created dynamically by the orchestrator at radio start time
  (POST /api/v1/radio/start). They contain hardware-specific fields (PCI address,
  CPU cores) that are only known at runtime.

  Do NOT pre-render these here — the orchestrator owns them.
*/ -}}
