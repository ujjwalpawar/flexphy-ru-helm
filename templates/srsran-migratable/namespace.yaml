{{- /*
  Namespace is created externally via:
    helm upgrade --install ... --create-namespace
  Do NOT manage it as a hook — before-hook-creation would delete the namespace
  on every upgrade, destroying Helm release tracking secrets stored inside it.
*/ -}}
