{{- /*
  Shadow DU-Low ConfigMaps are created dynamically by the orchestrator at migrate time
  (POST /api/v1/radio/migrate). They contain hardware-specific fields (PCI address,
  CPU cores on the target node) that are only known at runtime.

  Do NOT pre-render these here — the orchestrator owns them.
*/ -}}
